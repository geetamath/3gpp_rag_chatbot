# --- Always required (demo mode: EMBEDDING_BACKEND=tfidf, LLM_BACKEND=echo) ---
scikit-learn>=1.3
rank-bm25>=0.2.2
python-docx>=1.1.0
pypdf>=4.0.0
numpy>=1.24

# --- Production mode: EMBEDDING_BACKEND=sentence-transformers ---
sentence-transformers>=2.7.0
faiss-cpu>=1.7.4

# --- Production mode: LLM_BACKEND=ollama ---
# No extra pip package needed -- talks to Ollama over HTTP (urllib, stdlib).
# Install Ollama separately: https://ollama.com
#   ollama pull llama3.1:8b     (or mistral, etc.)
#   ollama serve

# --- Optional: simple web UI ---
gradio>=4.0
requests>=2.31.0

