# Minimal dependency set for the DEPLOYED WEB APP (Docker image).
# This intentionally excludes sentence-transformers / faiss-cpu / gradio --
# those pull in PyTorch + several GB of NVIDIA CUDA wheels that are
# completely unused when running with RAG_EMBEDDING_BACKEND=tfidf and
# RAG_LLM_BACKEND=groq (the deployment defaults). If you later switch to
# EMBEDDING_BACKEND=sentence-transformers, install requirements.txt instead
# (the full dev requirements file), on a machine with enough disk space.

fastapi==0.110.1
uvicorn[standard]==0.25.0
scikit-learn>=1.3
rank-bm25>=0.2.2
python-docx>=1.1.0
pypdf>=4.0.0
numpy>=1.24
